name: block_play_table_ui
description: Flutter Web UI for Block Play Table trusted-mode task orchestration.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  graphql_flutter: ^5.3.0
  http: ^1.2.2
  xterm: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
