# 项目 Agent 指令

## 回复语言

- 使用中文回复。

## 改动后的验证要求

- 任何项目改动，包括代码、配置、依赖、脚本、测试、文档或 UI 行为变更完成后，都必须执行完整端到端验证，不能只依赖静态检查或单元测试。
- 默认完整验证入口：
  - `make test`
  - `make e2e`
  - `npm run e2e`
- `npm run e2e` 是浏览器自动化测试门禁。提交或交付前必须启动可访问的本地 UI/Manager 环境，并确保 Playwright 覆盖的主要用户流程全部通过。
- 浏览器自动化测试需要覆盖并通过主要流程：项目创建、Worker 注册/编辑、任务创建与启动、任务日志/会话/结果展示、看板状态分组和事件刷新。
- 涉及真实 Agent 执行、Worker 运行时、Agent CLI 参数、任务 worktree 或发布流程的改动，还必须运行 `npm run e2e:real-agents`。
- 如果本地环境暂时无法运行完整端到端测试，不能把改动标记为已完成；必须在回复中明确说明未通过的命令、阻塞原因和需要补跑的验证。

## 浏览器自动化测试流程

1. 如依赖缺失，先运行 `npm install`。
2. 启动本地完整栈：`make run-local`。
3. 确认 UI 可访问：`http://localhost:18080`。
4. 运行浏览器自动化测试：`npm run e2e`。
5. 测试结束后按需清理本地栈：`make stop-local`。

## 开发约束

- 优先沿用项目现有结构、命令和测试入口。
- 修改测试门禁、Playwright 配置、Docker Compose、Manager/Worker 协议或 Vue UI 时，必须同步更新相关文档。
- 不要提交未验证的主流程改动；端到端测试失败时，应先修复失败原因，再交付结果。
